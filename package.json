{
  "dependencies": {
    "express": "^4.18.2",
    "express-handlebars": "^6.0.6",
    "firebase": "^9.14.0",
    "handlebars": "^4.7.7",
    "node": "^19.1.0",
	"serve-favicon": "^2.5.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "handlebars views/partials/userSideBar.handlebars -f public/userSideBar.js",
    "prestart": "npm run build"
  }
}
